---
title: Flags arrive as attributes
tags: [parts, css]
---
A flag part is not content — it lands as `data-<name>` on the fragment's root element, where CSS can reach it. The read-more fade on a truncated summary is a rule on a fact, and no template branches to produce it.
