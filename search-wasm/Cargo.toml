[package]
name = "grackle-search-wasm"
version = "0.1.0"
edition = "2021"

[lib]
crate-type = ["cdylib"]

[dependencies]
grackle-search-core = { path = "../search-core" }
postcard = { version = "1", features = ["alloc"] }
