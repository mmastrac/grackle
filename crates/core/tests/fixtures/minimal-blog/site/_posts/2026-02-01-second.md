---
title: Second
noindex: true
---

A post that asks search engines away, so the declared `robots` meta has
something to say on exactly one page — and `noindex` being ordinary declared
schema (§4e) is what lets front matter set it at all.
