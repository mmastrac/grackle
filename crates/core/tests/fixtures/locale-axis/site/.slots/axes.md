<!-- C4c: `axes` is a shell slot the ENGINE fills — `stream:axis`, the
language/theme switcher. This file must fill nothing: the fixture's pages
carry their two switchers, byte for byte, and the build reports the stem as
one no theme's shell leaves to the tree. Before C4c the fill landed in a slot
the binder had validated as a stream. -->

This prose must never reach a page.
