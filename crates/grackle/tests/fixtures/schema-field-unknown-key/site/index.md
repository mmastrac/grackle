---
title: A note
summary: One line about it.
---

The declaration governing this row carries a `default` key the engine never
read. That is a load error now, not a quiet demotion to `{ type = "string" }`.
