# Deliberately empty.
#
# This site exists to be MEASURED, not to show features off — field-notes is
# the kitchen sink, and `../raw` is this same site with nothing inherited.
# Every line here is a line a newcomer has to write before anything appears,
# so the count is the number to watch.
#
#   2026-07-20: 27 non-blank, non-comment lines.
#   2026-07-25: 0.
#
# `extends` defaults to "default", so the base config compiled into the binary
# (DESIGN.md §4d) supplies the three collections and their rules, the three
# markers, the `published` set, `/`, `/blog/`, the feed and the sitemap. A site
# with no `themes/` directory renders through the base theme the same way.
#
# The first thing a real site writes is `[site]` — the placeholder title and
# URL are the one part of the base nobody should keep.
