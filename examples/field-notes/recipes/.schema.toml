# §5b: typed fields for every row beneath this directory. An undeclared
# front-matter key under here is now a load error naming the file.
course = { type = "string" }
servings = { type = "int" }
minutes = { type = "int" }

# q40: list-of-records — each item is a map of the nested fields.
ingredients = { type = "records", fields = { amount = "string", name = "string", note = "string" } }
