---
title: Flags that CSS can see
tags: [css, parts]
---
Some facts about a post are not content. Truncation, a tree page, a card
that should fade — those land as `data-*` on the fragment root, where a
stylesheet can reach them without the theme inventing a branch.

Open this post under two looks and the same flag is there either way; only
the paint changes.
