---
title: Guide
order: 0
---
This subtree carries a `.section` marker, so every page under it renders with the section tree in a slot. The tree is the same recursive `outline_entry` fragment the page outline uses — only the framing differs.

`deep/` deliberately has no index of its own, so its label is an `<a>` with no `href`.
